@using Application.Categories.DTOs
@using Application.Common.Base.DTOs
@model PaginatedListDTO<CategoryListDTO>

@{
    ViewData["Title"] = "Kategorilerim";
    var currentPageUrl = $"{Context.Request.Path}{Context.Request.QueryString}";
    var clearFiltersUrl = Url.Action("Index", "Category", new { area = "Admin", role = ViewContext.RouteData.Values["role"] });
    var searchValue = Context.Request.Query["Search"].ToString();
    var companyId = Context.Request.Query["CompanyId"].ToString();
    var storeId = Context.Request.Query["StoreId"].ToString();
    var menuId = Context.Request.Query["MenuId"].ToString();
    var hasSearch = !string.IsNullOrWhiteSpace(searchValue);
    var hasCompany = !string.IsNullOrWhiteSpace(companyId);
    var hasStore = !string.IsNullOrWhiteSpace(storeId);
    var hasMenu = !string.IsNullOrWhiteSpace(menuId);
    var hasAnyFilter = hasSearch || hasCompany || hasStore || hasMenu;
    var isAdvancedFilterOpen = hasCompany || hasStore || hasMenu;
    var collapseClass = isAdvancedFilterOpen ? "collapse show" : "collapse";
    var companyFilterName = hasCompany && Model.FilterNames.TryGetValue(companyId, out var resolvedCompanyName) ? resolvedCompanyName : "Şirket";
    var storeFilterName = hasStore && Model.FilterNames.TryGetValue(storeId, out var resolvedStoreName) ? resolvedStoreName : "Dükkan";
    var menuFilterName = hasMenu && Model.FilterNames.TryGetValue(menuId, out var resolvedMenuName) ? resolvedMenuName : "Menü";
}

<div class="row">
    <div class="col-md-12">
        <div class="horizon-card">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h4 class="fw-bold mb-1">Kategorilerim</h4>
                    <p class="text-secondary small mb-0">Size ait menülerdeki kategorileri yönetin.</p>
                </div>
                <a asp-action="CreateCategory" asp-route-returnUrl="@currentPageUrl" class="btn btn-primary">
                    <i class="fas fa-plus me-2"></i>Kategori Ekle
                </a>
            </div>

            <div class="tm-filter-panel mb-4">
                <form asp-action="Index" method="get">
                    <div class="tm-filter-toolbar">
                        <div class="tm-filter-search">
                            <span class="tm-filter-search-icon"><i class="fas fa-search"></i></span>
                            <input type="text" name="Search" class="form-control" placeholder="Kategori adı ara..." value="@searchValue">
                        </div>
                        <div class="tm-filter-actions">
                            <button type="button" class="btn btn-outline-secondary" data-bs-toggle="collapse" data-bs-target="#advancedFilters" aria-expanded="@(isAdvancedFilterOpen ? "true" : "false")" aria-controls="advancedFilters">
                                <i class="fas fa-sliders-h me-1"></i>Detaylı Filtre
                            </button>
                            <a href="@clearFiltersUrl" class="btn btn-outline-secondary" title="Filtreleri Temizle">
                                <i class="fas fa-times me-1"></i>Temizle
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-search me-1"></i>Ara
                            </button>
                        </div>
                    </div>

                    @if (hasAnyFilter)
                    {
                        <div class="tm-active-filters">
                            <span class="tm-active-filters__label"><i class="fas fa-filter me-1"></i>Aktif filtreler</span>
                            @if (hasSearch) { <span class="tm-filter-chip"><i class="fas fa-search"></i>@searchValue</span> }
                            @if (hasCompany) { <span class="tm-filter-chip"><i class="fas fa-building"></i>@companyFilterName</span> }
                            @if (hasStore) { <span class="tm-filter-chip"><i class="fas fa-store"></i>@storeFilterName</span> }
                            @if (hasMenu) { <span class="tm-filter-chip"><i class="fas fa-book-open"></i>@menuFilterName</span> }
                        </div>
                    }

                    <div class="@collapseClass" id="advancedFilters">
                        <div class="tm-filter-advanced">
                            <div class="tm-filter-field tm-filter-field--scope">
                                <label class="tm-form-label">Kapsam</label>
                                <div class="btn-group w-100" role="group" aria-label="Kapsam seçimi">
                                    <input type="radio" class="btn-check" name="FilterType" id="filterCompany" value="company" autocomplete="off" @(!hasStore ? "checked" : "")>
                                    <label class="btn btn-outline-secondary" for="filterCompany"><i class="fas fa-building me-1"></i>Şirket</label>

                                    <input type="radio" class="btn-check" name="FilterType" id="filterStore" value="store" autocomplete="off" @(hasStore ? "checked" : "")>
                                    <label class="btn btn-outline-secondary" for="filterStore"><i class="fas fa-store me-1"></i>Dükkan</label>
                                </div>
                            </div>

                            <div class="tm-filter-field" id="companyContainer">
                                <label class="tm-form-label">Şirket Filtresi</label>
                                <div class="w-100">
                                    <select name="CompanyId" id="companySelect" class="form-select select2-ajax w-100" data-url="@Url.Action("GetCompaniesForSelect2", "Company", new { area = "Admin", role = ViewContext.RouteData.Values["role"] })" data-placeholder="Şirket Seçin...">
                                        @if (hasCompany) { <option value="@companyId" selected>@companyFilterName</option> }
                                    </select>
                                </div>
                            </div>

                            <div class="tm-filter-field" id="storeContainer" style="display: none;">
                                <label class="tm-form-label">Dükkan Filtresi</label>
                                <div class="w-100">
                                    <select name="StoreId" id="storeSelect" class="form-select select2-ajax w-100" data-url="@Url.Action("GetStoresForSelect2", "Store", new { area = "Admin", role = ViewContext.RouteData.Values["role"] })" data-placeholder="Dükkan Seçin...">
                                        @if (hasStore) { <option value="@storeId" selected>@storeFilterName</option> }
                                    </select>
                                </div>
                            </div>

                            <div class="tm-filter-field">
                                <label class="tm-form-label">Menü</label>
                                <div class="w-100">
                                    <select name="MenuId" id="menuSelect" class="form-select select2-ajax w-100" data-url="@Url.Action("GetMenusForSelect2", "Menu", new { area = "Admin", role = ViewContext.RouteData.Values["role"] })" data-placeholder="Menü Seçin...">
                                        @if (hasMenu) { <option value="@menuId" selected>@menuFilterName</option> }
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>

            <div class="table-responsive">
                <table class="table table-borderless align-middle table-hover">
                    <thead>
                        <tr class="text-secondary small text-uppercase border-bottom">
                            <th class="ps-3">Kategori Adı</th>
                            <th>Üst Kategori</th>
                            <th>Bağlı Olduğu Şirket / Dükkan</th>
                            <th class="tm-menu-column">Menü</th>
                            <th class="text-center">Ürün Sayısı</th>
                            <th>Sıra</th>
                            <th>Durum</th>
                            <th class="text-center">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model != null && Model.Items.Any())
                        {
                            foreach (var item in Model.Items)
                            {
                                <tr data-tummenu-click-url="@Url.Action("Details", new { id = item.Id, returnUrl = currentPageUrl })">
                                    <td class="ps-3">
                                        <div class="d-flex align-items-center">
                                            <div class="horizon-avatar bg-light text-primary me-3">
                                                @if (!string.IsNullOrEmpty(item.IconKey))
                                                {
                                                    <i class="@item.IconKey"></i>
                                                }
                                                else
                                                {
                                                    <i class="fas fa-tags"></i>
                                                }
                                            </div>
                                            <div>
                                                <span class="fw-bold text-dark d-block">@item.Title</span>
                                                @if (!string.IsNullOrEmpty(item.Description))
                                                {
                                                    <small class="text-muted">@item.Description</small>
                                                }
                                            </div>
                                        </div>
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.ParentName))
                                        {
                                            <span class="badge bg-light text-secondary border"><i class="fas fa-level-up-alt fa-rotate-90 me-1"></i> @item.ParentName</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted small">-</span>
                                        }
                                    </td>
                                    <td>
                                        @if (!string.IsNullOrEmpty(item.StoreName))
                                        {
                                            <div class="d-inline-flex align-items-center p-1 px-2 border rounded bg-light">
                                                <span class="badge bg-info bg-opacity-10 text-info me-2"><i class="fas fa-store"></i> Dükkan</span>
                                                <small class="text-dark fw-medium">@item.StoreName</small>
                                            </div>
                                        }
                                        else
                                        {
                                            <div class="d-inline-flex align-items-center p-1 px-2 border rounded bg-light">
                                                <span class="badge bg-primary bg-opacity-10 text-primary me-2"><i class="fas fa-building"></i> Şirket</span>
                                                <small class="text-dark fw-medium">@item.CompanyName</small>
                                            </div>
                                        }
                                    </td>
                                    <td class="text-secondary tm-menu-column">@item.MenuName</td>
                                    <td class="text-center">
                                        <span class="badge bg-light text-dark fw-normal">@item.ProductCount Ürün</span>
                                    </td>
                                    <td class="text-secondary">@item.SortOrder</td>
                                    <td>
                                        @if (item.IsActive)
                                        {
                                            <span class="badge bg-success">AKTİF</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-light text-secondary">PASİF</span>
                                        }
                                    </td>
                                    <td class="text-center text-nowrap">
                                        <a asp-action="Details" asp-route-id="@item.Id" asp-route-returnUrl="@currentPageUrl" class="btn btn-sm btn-outline-secondary me-1" title="Detaylar">
                                            <i class="fas fa-eye"></i>
                                        </a>
                                        <button type="button" class="btn btn-sm btn-outline-danger" data-bs-toggle="modal" data-bs-target="#deleteCatModal-@item.Id" title="Sil">
                                            <i class="fas fa-trash"></i>
                                        </button>

                                        <div class="modal fade" id="deleteCatModal-@item.Id" tabindex="-1">
                                            <div class="modal-dialog">
                                                <div class="modal-content">
                                                    <div class="modal-header">
                                                        <h5 class="modal-title">Kategoriyi Sil</h5>
                                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                                    </div>
                                                    <div class="modal-body">
                                                        <p><strong>@item.Title</strong> kategorisini menüden kaldırmak istediğinize emin misiniz?</p>
                                                        @if (item.ProductCount > 0)
                                                        {
                                                            <div class="alert alert-warning mb-0">
                                                                <i class="fas fa-exclamation-triangle me-1"></i>
                                                                Bu kategoride <strong>@item.ProductCount ürün</strong> bulunmaktadır.
                                                            </div>
                                                        }
                                                    </div>
                                                    <div class="modal-footer">
                                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">İptal</button>
                                                        <form asp-action="Delete" asp-route-id="@item.Id" asp-route-menuId="@item.MenuId" asp-route-returnUrl="@currentPageUrl" method="post" class="d-inline">
                                                            @Html.AntiForgeryToken()
                                                            <button type="submit" class="btn btn-danger">Evet, Kaldır</button>
                                                        </form>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr>
                                <td colspan="9" class="text-center py-5 text-secondary">
                                    <div class="mb-3">
                                        <span class="fa-stack fa-2x">
                                            <i class="fas fa-circle fa-stack-2x text-light"></i>
                                            <i class="fas fa-tags fa-stack-1x text-secondary opacity-50"></i>
                                        </span>
                                    </div>
                                    <h5>Kategori bulunamadı</h5>
                                    <p class="small">Aradığınız kriterlere uygun, size ait bir kategori yok.</p>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <tummenu-pagination index="@(Model?.Index ?? 1)"
                                pages="@(Model?.Pages ?? 0)"
                                has-previous="@(Model?.HasPrevious ?? false)"
                                has-next="@(Model?.HasNext ?? false)" />
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            function toggleFilterType() {
                if ($('#filterCompany').is(':checked')) {
                    $('#companyContainer').show();
                    $('#storeContainer').hide();
                    if ($('#storeSelect').val()) {
                        $('#storeSelect').val(null).trigger('change');
                    }
                } else {
                    $('#companyContainer').hide();
                    $('#storeContainer').show();
                    if ($('#companySelect').val()) {
                        $('#companySelect').val(null).trigger('change');
                    }
                }
            }

            $('input[name="FilterType"]').change(toggleFilterType);
            
            // Initial load
            if ($('#filterStore').is(':checked')) {
                $('#companyContainer').hide();
                $('#storeContainer').show();
            } else {
                $('#companyContainer').show();
                $('#storeContainer').hide();
            }

            $('.select2-ajax').each(function() {
                var $this = $(this);
                var url = $this.data('url');
                var placeholder = $this.data('placeholder');

                $this.select2({
                    theme: 'bootstrap-5',
                    placeholder: placeholder,
                    allowClear: true,
                    width: '100%',
                    ajax: {
                        url: url,
                        dataType: 'json',
                        delay: 250,
                        data: function(params) {
                            return {
                                search: params.term,
                                page: params.page || 1,
                                companyId: $('#companySelect').val(),
                                storeId: $('#storeSelect').val(),
                                menuId: $('#menuSelect').val()
                            };
                        },
                        processResults: function(data, params) {
                            params.page = params.page || 1;
                            return {
                                results: data.results,
                                pagination: {
                                    more: data.pagination.more
                                }
                            };
                        },
                        cache: true
                    }
                });
            });
        });
    </script>
}
