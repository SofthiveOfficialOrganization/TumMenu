@using Application.Menus.Commands
@model CreateMenuToStoreCommand

@{
    ViewData["Title"] = "Create Menu for Store";
}

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="horizon-card">
            <h4 class="fw-bold mb-4">Create Menu for Store</h4>
            
            <form asp-action="CreateToStore" method="post">
                <div class="mb-3">
                    <label asp-for="Title" class="form-label text-secondary small text-uppercase fw-bold">Menu Name</label>
                    <input asp-for="Title" class="form-control" placeholder="e.g. Lunch Menu" />
                    <span asp-validation-for="Title" class="text-danger small"></span>
                </div>
                
                <div class="mb-4">
                    <label asp-for="StoreId" class="form-label text-secondary small text-uppercase fw-bold">Store ID</label>
                    <input asp-for="StoreId" class="form-control" placeholder="Enter Store ID" />
                    <span asp-validation-for="StoreId" class="text-danger small"></span>
                </div>

                <div class="d-flex justify-content-end gap-2">
                    <a asp-action="MyMenus" class="btn btn-light text-secondary">Cancel</a>
                    <button type="submit" class="btn btn-primary">Create Menu</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
